table:
  name: roles
  schema: public
is_enum: true
array_relationships:
- name: users_roles
  using:
    foreign_key_constraint_on:
      column: role
      table:
        name: users_roles
        schema: public
